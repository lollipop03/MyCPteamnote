#include <bits/stdc++.h>

using namespace std;
#define ll long long

void init(){
    cin.tie(0);
    cout.tie(0);
    ios_base::sync_with_stdio(false);
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
}

int solve() {
    int ans = 0;

    cout << ans << '\n';

    return 0;
}

int main() {
    init();
    int T;
    cin >> T;
    
    while (T--) {
        solve();
    }
    
    return 0;
}